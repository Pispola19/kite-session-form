# 01-golden

Deriva da `/Users/PER_TEST/raccolta_dati_K_test/public_dam_gateway/test_payload.json`.

Tipo: esempio locale di payload legacy per public DAM gateway.

Anonimizzazione: nessun dato personale presente nel file sorgente; `location` e `note` sono valori canary locali.

Nessun endpoint chiamato. Nessun submit eseguito.

Hash parity: not verifiable. Il `message_id` e' un valore canary gia' presente nel file locale, non e' dimostrato che sia stato generato da `app.js`.
